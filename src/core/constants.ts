export const BRICK_HEIGHT = 50;
export const FULL_BRICK_HEIGHT = 210;
export const HALF_BRICK_HEIGHT = 100;
export const HEAD_JOINT_SIZE = 10;
export const BED_JOINT_SIZE = 12.5;
export const COURSE_HEIGHT = BRICK_HEIGHT + BED_JOINT_SIZE;

/**
 * The size of a full brick module. Size of a full brick plus the size of the head joint.
 */
export const FULL_BRICK_MODULE_SIZE = FULL_BRICK_HEIGHT + HEAD_JOINT_SIZE;
/**
 * The size of a half brick module. Size of a half brick plus the size of the head joint.
 */
export const HALF_BRICK_MODULE_SIZE = HALF_BRICK_HEIGHT + HEAD_JOINT_SIZE;
